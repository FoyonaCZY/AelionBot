import {GamePhaseStatus,electionLabel} from './GamePhaseStatus';
import {GameDiagnostics} from './GameDiagnostics';
import {useEffect,useRef,useState} from 'react';
import {Avatar} from './ui';
import type {GameView,GameAction} from './game-types';
import {ROLE_NAMES as roles,PHASE_NAMES,ACTION_NAMES} from './game-boards';
import {GameActionControls} from './GameActionControls';
export function WerewolfTable({game,onUpdate,onMutationStart,syncError,omniscient,onOmniscient,onClose}:{game:GameView;onMutationStart:()=>void;syncError:string;onUpdate:(v:GameView)=>void;omniscient:boolean;onOmniscient:()=>void;onClose:()=>void}){
 const [now,setNow]=useState(Date.now()),[confirmStop,setConfirmStop]=useState(false),[diagnostics,setDiagnostics]=useState(false);useEffect(()=>{const timer=setInterval(()=>setNow(Date.now()),200);return()=>clearInterval(timer);},[]);
 const [text,setText]=useState(''),[target,setTarget]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const feed=useRef<HTMLDivElement>(null);
 const request=game.pending[0];const self=game.seats.find(p=>p.id===game.humanId);const seconds=game.clock&&(game.clock.deadlineAt!==undefined||game.clock.remainingMs!==undefined)?Math.max(0,Math.ceil((game.status==='paused'?(game.clock.remainingMs||0):(game.clock.deadlineAt||now)-now)/1000)):null;const speaker=game.seats.find(p=>p.id===game.clock?.seatId);const death=game.logs.find(e=>!e.seatId&&(!!self&&e.text.includes(self.name)&&(e.text.includes(' 出局。')||e.text.includes(' 被放逐。')))); useEffect(()=>{setText('');setTarget('');setError('');},[request?.id]);useEffect(()=>{if(feed.current)feed.current.scrollTop=feed.current.scrollHeight;},[game.logs.length]);
 async function perform(action:()=>Promise<GameView>){if(busy||syncError)return;onMutationStart();setBusy(true);setError('');try{onUpdate(await action());}catch(e){setError((e as Error).message);}finally{setBusy(false);}}
 const control=(action:'pause'|'resume'|'stop')=>perform(()=>window.aelion.games.control({id:game.id,action}));
 const submit=(action:GameAction)=>perform(()=>window.aelion.games.act({id:game.id,requestId:request!.id,action}));
 return <><div className="gg-table-heading"><h2>{game.status==='finished'?(game.winner==='wolves'?'狼人获胜':game.winner==='village'?'好人获胜':'对局结束'):'狼人杀 · '+(game.board==='guard12'?'预女猎守':game.board==='standard12'?'预女猎白':'七人局')}</h2><div className="wg-actions"><button className="secondary-button" onClick={()=>setDiagnostics(v=>!v)}>运行记录</button>{!game.humanId&&<button className="secondary-button" aria-pressed={omniscient} onClick={onOmniscient}>{omniscient?'全知视角':'公共视角'}</button>}{game.status!=='finished'&&<><button className="secondary-button" disabled={busy||!!syncError||!!syncError} onClick={()=>void control(game.status==='paused'?'resume':'pause')}>{game.status==='paused'?'继续':'暂停'}</button><button className="secondary-button" disabled={busy||!!syncError||!!syncError} onClick={()=>setConfirmStop(true)}>结束对局</button></>}</div></div>{syncError&&<div role="alert" className="wg-confirm">连接中断，当前显示最后一次同步的状态。{syncError}</div>}{diagnostics&&<GameDiagnostics game={game}/>} {confirmStop&&<div className="wg-confirm" role="alert"><span>结束后本局不计胜负，并保留操作记录。</span><button className="secondary-button" onClick={()=>setConfirmStop(false)}>取消</button><button className="primary-button" disabled={busy||!!syncError||!!syncError} onClick={()=>{setConfirmStop(false);void control('stop');}}>确认结束</button></div>}{self&&!self.alive&&game.status!=='finished'&&<div className="wg-death" role="status"><strong>你已出局</strong><span>{death?.text||'你已出局。'} {request?'请完成遗言、技能或警徽操作。':'可继续观战。'}</span></div>}<div className="gg-table"><aside className="gg-seats"><div className="gg-section-label">玩家 <span>{game.seats.filter(p=>p.alive).length} 人存活</span></div>{game.seats.map((p,i)=><div key={p.id} className="gg-seat" style={{opacity:p.alive?1:.5}}><span className="gg-seat-number">{i+1}</span><Avatar bot={p} size={32}/><div><strong>{p.name}{p.human&&p.name!=='你'?' · 你':''}</strong><small>{p.role?roles[p.role]:''}{game.sheriffId===p.id?' · 警长':''}{p.canVote===false?' · 无投票权':''}{!p.alive?' · 已出局':''}</small>{electionLabel(game,p.id)&&<span className="wg-election-tag" data-active={game.candidates?.includes(p.id)||undefined}>{electionLabel(game,p.id)}</span>}</div></div>)}</aside><section className="gg-stage"><GamePhaseStatus game={game} seconds={seconds} syncError={syncError}/><div className="gg-transcript" ref={feed}>{game.logs.map(e=>{const player=game.seats.find(p=>p.id===e.seatId);return player?<article className="gg-message group-message" key={e.id}><Avatar bot={player} size={32}/><div><strong>{player.name}</strong><p className="group-message-bubble">{e.text}</p></div></article>:<div className="gg-system" key={e.id}>{e.time&&<time>{new Date(e.time).toLocaleTimeString('zh-CN',{hour:'2-digit',minute:'2-digit',second:'2-digit'})} · </time>}{e.text}</div>;})}</div><footer className="gg-controls"><div className="wg-input">{(error||game.error)&&<p role="alert" className="group-error">{error||game.error}</p>}{game.status==='finished'?<button className="primary-button" onClick={onClose}>返回群聊</button>:game.status==='paused'?<span>对局已暂停</span>:!request?<span>{game.seats.find(p=>p.id===game.humanId)?.alive===false?'你已出局，可继续观战。':'等待 AI 行动…'}</span>:<GameActionControls request={request} game={game} busy={busy||!!syncError} submit={submit}/>}</div></footer></section></div></>;
}
