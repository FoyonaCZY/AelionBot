export type WebPreviewSource={kind:'url';url:string;location?:'vm'|'host';botId?:string}|{kind:'document';format:'html'|'pdf';name:string;attachmentId?:string;content?:string;dataUrl?:string;workspace?:{botId:string;path:string}};
export interface WebPreviewState{id:string;url:string;title:string;loading:boolean;canBack:boolean;canForward:boolean;error?:string;}
export function webPreviewUrl(value:unknown){if(typeof value!=='string'||value.length>4096)throw Error('网页地址无效');const url=new URL(value);if(!['http:','https:'].includes(url.protocol)||url.username||url.password)throw Error('网页预览仅支持不含账号密码的 HTTP/HTTPS 地址');return url;}
export function vmPreviewPort(value:unknown){const url=webPreviewUrl(value);if(url.protocol!=='http:'||!['localhost','127.0.0.1','[::1]','0.0.0.0'].includes(url.hostname))throw Error('VM 预览请使用 http://localhost:端口/路径');const port=Number(url.port||80);if(!Number.isInteger(port)||port<1024||port>65535)throw Error('VM 前端端口需要在 1024–65535 之间');return {url,port};}
export function feedbackWebUrl(value:string){try{const url=new URL(value);for(const key of [...url.searchParams.keys()])if(/token|secret|password|code|auth/i.test(key))url.searchParams.set(key,'[redacted]');if(/token|secret|password|auth/i.test(url.hash))url.hash='';return url.href;}catch{return '';}}