# AelionBot v0.13.1

本版调整任务默认限制，仅提供 Windows x64 安装包。

- 执行轮数、最长执行时间默认均为不限，使用 `0` 表示不限。
- 最长执行时间设为 `0` 时不再启动任务超时计时器；仍可设置正数限制。
- 已保存的自定义设置继续保留，可在运行设置中点击“恢复默认”应用新默认值。

## 下载

- Windows x64：`AelionBot-Setup-0.13.1-x64.exe`，也可通过客户端检查更新。
- 附带自动更新清单、差分更新文件和 `SHA256SUMS.txt`。
